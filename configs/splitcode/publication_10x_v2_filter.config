# Qualitatively aligned 10x Chromium v2 benchmark.
#
# Seqproc uses the 26-base R1 geometry only to reject pairs whose R1 is too
# short; the emitted R1 and R2 are otherwise unchanged.  Express that workload
# as a length filter, without asking splitcode to perform an unused extraction.
@filter-len 26,0
