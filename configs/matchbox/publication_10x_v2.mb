# Semantically aligned 10x v2 benchmark: retain separate BC and UMI regions,
# emit their enclosing 26-base region in R1, and retain R2.
if read.r1 matches {
    [bcumi:(bc:|16| umi:|10|)] => {
        bcumi.out!('mb_r1.fq')
        read.r2.out!('mb_r2.fq')
    }
}
